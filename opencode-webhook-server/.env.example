# Required: directory opencode agents run in (no default - server fails fast without it)
OPENCODE_WORKSPACE=/path/to/your/projects

# Optional overrides (defaults shown)
#OPENCODE_BIN="$HOME/.opencode/bin/opencode"
#WEBHOOK_PORT=8080
DEFAULT_MODEL_ID=Qwen3_6-35B-A3B-MTP
DEFAULT_MODEL_PROVIDER=llama.cpp
#AGENTS_DIR=./agents
#SHUTDOWN_GRACE_MS=5000
